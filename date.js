/* 
module.exports.getDate = getDate = function (){
    const today = new Date();
    const options = {
        weekday: 'long',
        day: 'numeric',
        month: 'long',
    };
   return today.toLocaleDateString("nl-BE", options);

}

module.exports.getYear = getYear = function (){
    const today = new Date();
    const options = {
        year: 'numeric',
    };
    return today.toLocaleDateString("nl-BE", options);

} */

